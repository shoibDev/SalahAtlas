spring:
  application:
    name: project-backend

  config:
    import: application-secret.yml

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Sensitive information has been moved to application-secret.yml
# This includes:
# - Database configuration
# - Email settings
# - JWT security settings
# - Frontend URLs
